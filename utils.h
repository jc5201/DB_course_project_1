#ifndef UTILS_H_
#define UTILS_H_

#include<fstream>
#include<vector>
#include<string>

using namespace std;

vector<string> ReadFile(char * filename);
string trim(const string& str);

#endif 
